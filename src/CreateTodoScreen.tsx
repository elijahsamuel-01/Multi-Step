import React from "react";
import { styled } from "styled-components";

const CreateTodoScreen = () => {
  return (
    <div>
      <Container>CreateTodoScreen</Container>
    </div>
  );
};

export default CreateTodoScreen;

const Container = styled.div`
  width: 100%;
`;
